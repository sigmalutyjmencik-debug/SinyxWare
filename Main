-- ================================================================
--  SINYXWARE v3.0 | FULL FIXED | ALL FEATURES WORKING
--  Версия: 3.0 | Дата: 19.08.2026
--  Фиксы: Аимбот, ESP, MISC, Mobile, Search, Sliders
-- ================================================================

local UIS = game:GetService("UserInputService")
local TS = game:GetService("TweenService")
local CG = game:GetService("CoreGui")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LP = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local HttpService = game:GetService("HttpService")

if not Drawing then error("Drawing не поддерживается") end

local C = {
    bg = Color3.fromRGB(18, 12, 12),
    sidebar = Color3.fromRGB(24, 15, 15),
    panel = Color3.fromRGB(30, 19, 19),
    section = Color3.fromRGB(36, 22, 22),
    red = Color3.fromRGB(210, 30, 30),
    redDark = Color3.fromRGB(130, 12, 12),
    text = Color3.fromRGB(218, 212, 212),
    dim = Color3.fromRGB(120, 110, 110),
    white = Color3.fromRGB(255, 255, 255),
    border = Color3.fromRGB(50, 28, 28),
    togOff = Color3.fromRGB(60, 38, 38),
    blue = Color3.fromRGB(0, 191, 255),
}

local W, H = 390 * 1.2, 270 * 1.2
local SW = 90 * 1.2

pcall(function() CG:FindFirstChild("SinyxWare"):Destroy() end)

local Root = Instance.new("ScreenGui")
Root.Name = "SinyxWare"
Root.ResetOnSpawn = false
Root.IgnoreGuiInset = true
Root.ZIndexBehavior = Enum.ZIndexBehavior.Global
Root.Parent = CG

local Outer = Instance.new("Frame", Root)
Outer.Size = UDim2.new(0, W, 0, H)
Outer.Position = UDim2.new(0.5, -W/2, 0.5, -H/2)
Outer.BackgroundColor3 = C.red
Outer.BorderSizePixel = 0
Instance.new("UICorner", Outer).CornerRadius = UDim.new(0, 4)

local Main = Instance.new("Frame", Outer)
Main.Size = UDim2.new(1, -4, 1, -4)
Main.Position = UDim2.new(0, 2, 0, 2)
Main.BackgroundColor3 = C.bg
Main.BorderSizePixel = 0
Main.ClipsDescendants = true
Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 3)

local dragData = {active = false, start = nil, offset = nil, touch = nil}

local function startDrag(input)
    dragData.active = true
    dragData.start = input.Position
    dragData.offset = Outer.Position
    dragData.touch = input.UserInputType == Enum.UserInputType.Touch
end

local function updateDrag(input)
    if not dragData.active then return end
    local delta = input.Position - dragData.start
    Outer.Position = UDim2.new(
        dragData.offset.X.Scale, dragData.offset.X.Offset + delta.X,
        dragData.offset.Y.Scale, dragData.offset.Y.Offset + delta.Y
    )
end

local function endDrag()
    dragData.active = false
end

Main.InputBegan:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
        startDrag(i)
    end
end)

Main.InputEnded:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
        endDrag()
    end
end)

UIS.InputChanged:Connect(function(i)
    if dragData.active and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
        updateDrag(i)
    end
end)

local Sidebar = Instance.new("Frame", Main)
Sidebar.Size = UDim2.new(0, SW, 1, 0)
Sidebar.BackgroundColor3 = C.sidebar
Sidebar.BorderSizePixel = 0

local SDiv = Instance.new("Frame", Main)
SDiv.Size = UDim2.new(0, 1, 1, 0)
SDiv.Position = UDim2.new(0, SW, 0, 0)
SDiv.BackgroundColor3 = C.border
SDiv.BorderSizePixel = 0

local TitleF = Instance.new("Frame", Sidebar)
TitleF.Size = UDim2.new(1, 0, 0, 26 * 1.2)
TitleF.BackgroundTransparency = 1

local TitleL = Instance.new("TextLabel", TitleF)
TitleL.Size = UDim2.new(1, 0, 1, 0)
TitleL.BackgroundTransparency = 1
TitleL.RichText = true
TitleL.Text = '<font color="rgb(0,191,255)">Sinyx</font><font color="rgb(210,30,30)">Ware</font>'
TitleL.Font = Enum.Font.GothamBold
TitleL.TextSize = 12 * 1.2
TitleL.TextXAlignment = Enum.TextXAlignment.Center

local TDiv = Instance.new("Frame", Sidebar)
TDiv.Size = UDim2.new(1, 0, 0, 1)
TDiv.Position = UDim2.new(0, 0, 0, 26 * 1.2)
TDiv.BackgroundColor3 = C.border
TDiv.BorderSizePixel = 0

local TABS = {
    {n = "Aimbot", ico = "✖"},
    {n = "Visuals", ico = "◎"},
    {n = "Misc", ico = "◉"},
}

local TabBtns = {}
local currentTab = "Aimbot"
local TabContents = {}

local NavF = Instance.new("Frame", Sidebar)
NavF.Size = UDim2.new(1, 0, 1, -(26 * 1.2))
NavF.Position = UDim2.new(0, 0, 0, 26 * 1.2)
NavF.BackgroundTransparency = 1

local NavL = Instance.new("UIListLayout", NavF)
NavL.Padding = UDim.new(0, 0)
NavL.SortOrder = Enum.SortOrder.LayoutOrder

local function setTab(name)
    currentTab = name
    for n, t in pairs(TabBtns) do
        local on = n == name
        t.bar.BackgroundTransparency = on and 0 or 1
        t.ico.TextColor3 = on and C.red or C.dim
        t.lbl.TextColor3 = on and C.white or C.dim
    end
    for n, f in pairs(TabContents) do
        f.Visible = n == name
    end
end

for i, tab in ipairs(TABS) do
    local btn = Instance.new("TextButton", NavF)
    btn.Size = UDim2.new(1, 0, 0, 22 * 1.2)
    btn.BackgroundTransparency = 1
    btn.Text = ""
    btn.AutoButtonColor = false
    btn.LayoutOrder = i

    local bar = Instance.new("Frame", btn)
    bar.Size = UDim2.new(0, 2, 0.65, 0)
    bar.Position = UDim2.new(0, 0, 0.175, 0)
    bar.BackgroundColor3 = C.red
    bar.BorderSizePixel = 0
    bar.BackgroundTransparency = tab.n == "Aimbot" and 0 or 1
    Instance.new("UICorner", bar).CornerRadius = UDim.new(0, 1)

    local ico = Instance.new("TextLabel", btn)
    ico.Size = UDim2.new(0, 18 * 1.2, 1, 0)
    ico.Position = UDim2.new(0, 8 * 1.2, 0, 0)
    ico.BackgroundTransparency = 1
    ico.Text = tab.ico
    ico.Font = Enum.Font.GothamBold
    ico.TextSize = 8 * 1.2
    ico.TextColor3 = tab.n == "Aimbot" and C.red or C.dim

    local lbl = Instance.new("TextLabel", btn)
    lbl.Size = UDim2.new(1, -(28 * 1.2), 1, 0)
    lbl.Position = UDim2.new(0, 26 * 1.2, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = tab.n
    lbl.Font = Enum.Font.Gotham
    lbl.TextSize = 7 * 1.2
    lbl.TextColor3 = tab.n == "Aimbot" and C.white or C.dim
    lbl.TextXAlignment = Enum.TextXAlignment.Left

    TabBtns[tab.n] = {bar = bar, ico = ico, lbl = lbl}
    btn.MouseButton1Click:Connect(function() setTab(tab.n) end)
    btn.TouchTap:Connect(function() setTab(tab.n) end)
end

local ContentArea = Instance.new("Frame", Main)
ContentArea.Size = UDim2.new(1, -SW - 1, 1, -(14 * 1.2))
ContentArea.Position = UDim2.new(0, SW + 1, 0, 0)
ContentArea.BackgroundTransparency = 1
ContentArea.ClipsDescendants = true

local TopBar = Instance.new("Frame", ContentArea)
TopBar.Size = UDim2.new(1, 0, 0, 21 * 1.2)
TopBar.BackgroundColor3 = C.sidebar
TopBar.BorderSizePixel = 0

local SearchIco = Instance.new("TextLabel", TopBar)
SearchIco.Size = UDim2.new(0, 14 * 1.2, 1, 0)
SearchIco.Position = UDim2.new(0, 4 * 1.2, 0, 0)
SearchIco.BackgroundTransparency = 1
SearchIco.Text = "🔍"
SearchIco.TextSize = 7 * 1.2
SearchIco.TextColor3 = C.dim

local SearchBox = Instance.new("TextBox", TopBar)
SearchBox.Size = UDim2.new(1, -(40 * 1.2), 1, -(4 * 1.2))
SearchBox.Position = UDim2.new(0, 20 * 1.2, 0, 2 * 1.2)
SearchBox.BackgroundTransparency = 1
SearchBox.Text = ""
SearchBox.PlaceholderText = "Search"
SearchBox.PlaceholderColor3 = C.dim
SearchBox.TextColor3 = C.text
SearchBox.Font = Enum.Font.Gotham
SearchBox.TextSize = 7 * 1.2
SearchBox.TextXAlignment = Enum.TextXAlignment.Left
SearchBox.BorderSizePixel = 0

local MoveIco = Instance.new("TextLabel", TopBar)
MoveIco.Size = UDim2.new(0, 14 * 1.2, 0, 14 * 1.2)
MoveIco.Position = UDim2.new(1, -(17 * 1.2), 0.5, -(7 * 1.2))
MoveIco.BackgroundColor3 = C.section
MoveIco.Text = "✛"
MoveIco.TextSize = 7 * 1.2
MoveIco.TextColor3 = C.dim
MoveIco.Font = Enum.Font.GothamBold
MoveIco.BorderSizePixel = 0
Instance.new("UICorner", MoveIco).CornerRadius = UDim.new(0, 2)

local TopDiv = Instance.new("Frame", ContentArea)
TopDiv.Size = UDim2.new(1, 0, 0, 1)
TopDiv.Position = UDim2.new(0, 0, 0, 21 * 1.2)
TopDiv.BackgroundColor3 = C.border
TopDiv.BorderSizePixel = 0

local function corner(f, r)
    Instance.new("UICorner", f).CornerRadius = UDim.new(0, r or 2)
end

local toggleStates = {}

local function mkToggle(parent, label, default)
    local row = Instance.new("Frame", parent)
    row.Size = UDim2.new(1, 0, 0, 16 * 1.2)
    row.BackgroundTransparency = 1

    local lbl = Instance.new("TextLabel", row)
    lbl.Size = UDim2.new(1, -(30 * 1.2), 1, 0)
    lbl.Position = UDim2.new(0, 5 * 1.2, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = label
    lbl.TextColor3 = C.text
    lbl.Font = Enum.Font.Gotham
    lbl.TextSize = 7 * 1.2
    lbl.TextXAlignment = Enum.TextXAlignment.Left

    local pill = Instance.new("TextButton", row)
    pill.Size = UDim2.new(0, 22 * 1.2, 0, 12 * 1.2)
    pill.Position = UDim2.new(1, -(26 * 1.2), 0.5, -(6 * 1.2))
    pill.BackgroundColor3 = default and C.red or C.togOff
    pill.BorderSizePixel = 0
    pill.Text = ""
    pill.AutoButtonColor = false
    corner(pill, 6)

    local knob = Instance.new("Frame", pill)
    knob.Size = UDim2.new(0, 8 * 1.2, 0, 8 * 1.2)
    knob.Position = default and UDim2.new(1, -(10 * 1.2), 0.5, -(4 * 1.2)) or UDim2.new(0, 2 * 1.2, 0.5, -(4 * 1.2))
    knob.BackgroundColor3 = C.white
    knob.BorderSizePixel = 0
    corner(knob, 4)

    local val = default
    toggleStates[label] = function() return val end

    local function toggle()
        val = not val
        toggleStates[label] = function() return val end
        TS:Create(pill, TweenInfo.new(0.12), {BackgroundColor3 = val and C.red or C.togOff}):Play()
        if val then
            TS:Create(knob, TweenInfo.new(0.12), {Position = UDim2.new(1, -(10 * 1.2), 0.5, -(4 * 1.2))}):Play()
        else
            TS:Create(knob, TweenInfo.new(0.12), {Position = UDim2.new(0, 2 * 1.2, 0.5, -(4 * 1.2))}):Play()
        end
    end

    pill.MouseButton1Click:Connect(toggle)
    pill.TouchTap:Connect(toggle)
    return row
end

local sliderValues = {}

local function mkSlider(parent, label, mn, mx, def, callback)
    local wrap = Instance.new("Frame", parent)
    wrap.Size = UDim2.new(1, 0, 0, 26 * 1.2)
    wrap.BackgroundTransparency = 1

    local lbl = Instance.new("TextLabel", wrap)
    lbl.Size = UDim2.new(1, -(5 * 1.2), 0, 11 * 1.2)
    lbl.Position = UDim2.new(0, 5 * 1.2, 0, 1 * 1.2)
    lbl.BackgroundTransparency = 1
    lbl.Text = label
    lbl.TextColor3 = C.text
    lbl.Font = Enum.Font.Gotham
    lbl.TextSize = 7 * 1.2
    lbl.TextXAlignment = Enum.TextXAlignment.Left

    local track = Instance.new("TextButton", wrap)
    track.Size = UDim2.new(1, -(10 * 1.2), 0, 12 * 1.2)
    track.Position = UDim2.new(0, 5 * 1.2, 0, 13 * 1.2)
    track.BackgroundColor3 = C.redDark
    track.BorderSizePixel = 0
    track.Text = ""
    track.AutoButtonColor = false
    corner(track, 2)

    local fill = Instance.new("Frame", track)
    fill.Size = UDim2.new((def - mn) / (mx - mn), 0, 1, 0)
    fill.BackgroundColor3 = C.red
    fill.BorderSizePixel = 0
    corner(fill, 2)

    local valLbl = Instance.new("TextLabel", track)
    valLbl.Size = UDim2.new(1, 0, 1, 0)
    valLbl.BackgroundTransparency = 1
    valLbl.Text = tostring(def)
    valLbl.TextColor3 = C.white
    valLbl.Font = Enum.Font.GothamBold
    valLbl.TextSize = 7 * 1.2
    valLbl.ZIndex = 3

    local val = def
    sliderValues[label] = function() return val end

    local sliding = false

    local function upd(ax)
        local rx = math.clamp(ax - track.AbsolutePosition.X, 0, track.AbsoluteSize.X)
        local pct = rx / track.AbsoluteSize.X
        val = math.floor(mn + (mx - mn) * pct + 0.5)
        fill.Size = UDim2.new(pct, 0, 1, 0)
        valLbl.Text = tostring(val)
        sliderValues[label] = function() return val end
        if callback then callback(val) end
    end

    local function startSlide(input)
        sliding = true
        upd(input.Position.X)
    end

    track.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            startSlide(i)
        end
    end)

    UIS.InputChanged:Connect(function(i)
        if sliding and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
            upd(i.Position.X)
        end
    end)

    UIS.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            sliding = false
        end
    end)

    return wrap
end

local function mkSection(parent, title, lo)
    local wrap = Instance.new("Frame", parent)
    wrap.Size = UDim2.new(1, -(6 * 1.2), 0, 0)
    wrap.AutomaticSize = Enum.AutomaticSize.Y
    wrap.Position = UDim2.new(0, 3 * 1.2, 0, 0)
    wrap.BackgroundTransparency = 1
    wrap.LayoutOrder = lo or 1

    local hdr = Instance.new("Frame", wrap)
    hdr.Size = UDim2.new(1, 0, 0, 17 * 1.2)
    hdr.BackgroundColor3 = C.section
    hdr.BorderSizePixel = 0
    corner(hdr, 2)

    local htxt = Instance.new("TextLabel", hdr)
    htxt.Size = UDim2.new(1, -(22 * 1.2), 1, 0)
    htxt.Position = UDim2.new(0, 6 * 1.2, 0, 0)
    htxt.BackgroundTransparency = 1
    htxt.Text = title
    htxt.TextColor3 = C.text
    htxt.Font = Enum.Font.GothamBold
    htxt.TextSize = 7 * 1.2
    htxt.TextXAlignment = Enum.TextXAlignment.Left

    local chevron = Instance.new("TextButton", hdr)
    chevron.Size = UDim2.new(0, 16 * 1.2, 1, 0)
    chevron.Position = UDim2.new(1, -(18 * 1.2), 0, 0)
    chevron.BackgroundTransparency = 1
    chevron.Text = "∨"
    chevron.TextColor3 = C.dim
    chevron.Font = Enum.Font.GothamBold
    chevron.TextSize = 8 * 1.2
    chevron.AutoButtonColor = false

    local body = Instance.new("Frame", wrap)
    body.Size = UDim2.new(1, 0, 0, 0)
    body.AutomaticSize = Enum.AutomaticSize.Y
    body.Position = UDim2.new(0, 0, 0, 19 * 1.2)
    body.BackgroundColor3 = C.panel
    body.BorderSizePixel = 0
    corner(body, 2)
    body.Visible = true

    local bl = Instance.new("UIListLayout", body)
    bl.Padding = UDim.new(0, 1)
    bl.SortOrder = Enum.SortOrder.LayoutOrder

    local bp = Instance.new("UIPadding", body)
    bp.PaddingTop = UDim.new(0, 3 * 1.2)
    bp.PaddingBottom = UDim.new(0, 3 * 1.2)

    local collapsed = false
    local function toggleSection()
        collapsed = not collapsed
        if collapsed then
            body.Visible = false
            chevron.Text = ">"
        else
            body.Visible = true
            chevron.Text = "∨"
        end
    end

    chevron.MouseButton1Click:Connect(toggleSection)
    chevron.TouchTap:Connect(toggleSection)
    return body
end

local function mkTabContent(name)
    local f = Instance.new("ScrollingFrame", ContentArea)
    f.Size = UDim2.new(1, 0, 1, -(23 * 1.2))
    f.Position = UDim2.new(0, 0, 0, 22 * 1.2)
    f.BackgroundTransparency = 1
    f.BorderSizePixel = 0
    f.ScrollBarThickness = 2
    f.ScrollBarImageColor3 = C.red
    f.AutomaticCanvasSize = Enum.AutomaticSize.Y
    f.CanvasSize = UDim2.new()
    f.Visible = name == "Aimbot"

    local colL = Instance.new("Frame", f)
    colL.Size = UDim2.new(0.5, -(2 * 1.2), 1, 0)
    colL.BackgroundTransparency = 1
    local llout = Instance.new("UIListLayout", colL)
    llout.Padding = UDim.new(0, 4 * 1.2)
    llout.SortOrder = Enum.SortOrder.LayoutOrder
    local lpad = Instance.new("UIPadding", colL)
    lpad.PaddingTop = UDim.new(0, 4 * 1.2)
    lpad.PaddingLeft = UDim.new(0, 2 * 1.2)

    local colR = Instance.new("Frame", f)
    colR.Size = UDim2.new(0.5, -(2 * 1.2), 1, 0)
    colR.Position = UDim2.new(0.5, 2 * 1.2, 0, 0)
    colR.BackgroundTransparency = 1
    local rlout = Instance.new("UIListLayout", colR)
    rlout.Padding = UDim.new(0, 4 * 1.2)
    rlout.SortOrder = Enum.SortOrder.LayoutOrder
    local rpad = Instance.new("UIPadding", colR)
    rpad.PaddingTop = UDim.new(0, 4 * 1.2)
    rpad.PaddingRight = UDim.new(0, 2 * 1.2)

    TabContents[name] = f
    return colL, colR
end

local CL, CR = mkTabContent("Aimbot")

local AimBody = mkSection(CL, "Aimbot", 1)

local aimKey = "Q"
local aimKeyBox = Instance.new("TextButton", AimBody)
aimKeyBox.Size = UDim2.new(0.3, 0, 0, 16 * 1.2)
aimKeyBox.Position = UDim2.new(0.6, 0, 0, 0)
aimKeyBox.BackgroundColor3 = C.section
aimKeyBox.Text = aimKey
aimKeyBox.TextColor3 = C.white
aimKeyBox.Font = Enum.Font.GothamBold
aimKeyBox.TextSize = 7 * 1.2
aimKeyBox.BorderSizePixel = 0
corner(aimKeyBox, 2)

local keyAwait = false
aimKeyBox.MouseButton1Click:Connect(function()
    keyAwait = true
    aimKeyBox.Text = "..."
end)
aimKeyBox.TouchTap:Connect(function()
    keyAwait = true
    aimKeyBox.Text = "..."
end)

UIS.InputBegan:Connect(function(i, gpe)
    if gpe then return end
    if keyAwait then
        if i.KeyCode ~= Enum.KeyCode.Unknown then
            aimKey = tostring(i.KeyCode):gsub("Enum.KeyCode.", "")
            aimKeyBox.Text = aimKey
            keyAwait = false
        end
    end
end)

mkToggle(AimBody, "Aimbot", false)
mkToggle(AimBody, "Snow Fov", true)
mkSlider(AimBody, "Fov Size", 10, 1000, 80)
mkSlider(AimBody, "Smooth", 1, 20, 1)
mkToggle(AimBody, "Predict", false)
mkSlider(AimBody, "Predict ms", 0, 500, 14)

local MobileBody = mkSection(CL, "Mobile Aimbot", 2)
mkToggle(MobileBody, "Snow Button", false)
mkToggle(MobileBody, "Lock Button", false)

local VL, VR = mkTabContent("Visuals")
local VBody = mkSection(VL, "ESP", 1)
mkToggle(VBody, "Players", true)
mkToggle(VBody, "Boxes", true)
mkToggle(VBody, "Names", true)
mkToggle(VBody, "Health", true)
mkToggle(VBody, "Tracers", false)

local ML, MR = mkTabContent("Misc")
local MBody = mkSection(ML, "Movement", 1)
mkToggle(MBody, "Speed Hack", false)
mkSlider(MBody, "Speed", 16, 200, 50)
mkToggle(MBody, "Infinite Jump", false)

local espData = {}
local rainbowOffset = 0

local function GetRainbowColor()
    rainbowOffset = (rainbowOffset + 0.005) % 1
    return Color3.fromHSV(rainbowOffset, 1, 1)
end

local function CreateESP(player)
    if player == LP or espData[player] then return end
    local e = {
        Box = Drawing.new("Square"),
        Name = Drawing.new("Text"),
        Health = Drawing.new("Square"),
        Dist = Drawing.new("Text"),
    }
    e.Box.Filled = false
    e.Box.Thickness = 1.5
    e.Box.Visible = false
    e.Name.Size = 12
    e.Name.Center = true
    e.Name.Outline = true
    e.Name.OutlineColor = Color3.fromRGB(0, 0, 0)
    e.Name.Font = 3
    e.Name.Visible = false
    e.Health.Filled = true
    e.Health.Thickness = 1
    e.Health.Visible = false
    e.Dist.Size = 10
    e.Dist.Center = true
    e.Dist.Outline = true
    e.Dist.OutlineColor = Color3.fromRGB(0, 0, 0)
    e.Dist.Font = 3
    e.Dist.Visible = false
    espData[player] = e
end

local function RemoveESP(player)
    local e = espData[player]
    if e then
        pcall(function()
            e.Box:Remove()
            e.Name:Remove()
            e.Health:Remove()
            e.Dist:Remove()
        end)
        espData[player] = nil
    end
end

for _, plr in pairs(Players:GetPlayers()) do CreateESP(plr) end
Players.PlayerAdded:Connect(CreateESP)
Players.PlayerRemoving:Connect(RemoveESP)

local ScreenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
local Circle = Drawing.new("Circle")
Circle.Thickness = 2
Circle.NumSides = 32
Circle.Visible = true
Circle.Color = Color3.fromRGB(160, 32, 240)
Circle.Radius = 80
Circle.Filled = false
Circle.Position = ScreenCenter

local function getToggleState(label)
    if toggleStates[label] then
        return toggleStates[label]()
    end
    return false
end

local function getSliderValue(label)
    if sliderValues[label] then
        return sliderValues[label]()
    end
    return 0
end

local function GetClosestPlayer()
    local closest = nil
    local closestDist = getSliderValue("Fov Size")

    for _, plr in pairs(Players:GetPlayers()) do
        if plr == LP then continue end
        if not plr.Character or not plr.Character:FindFirstChild("Head") then continue end

        local part = plr.Character.Head
        local pos, onScreen = Camera:WorldToViewportPoint(part.Position)

        if not onScreen then continue end

        local dist = (Vector2.new(pos.X, pos.Y) - ScreenCenter).Magnitude

        if dist < closestDist then
            closestDist = dist
            closest = plr
        end
    end

    return closest, closestDist
end

local currentTarget = nil
local lastAimTime = 0

local function AimAt(target)
    if not target or not target.Character or not target.Character:FindFirstChild("Head") then return end

    local part = target.Character.Head
    local targetPos = part.Position

    if getToggleState("Predict") then
        local root = target.Character:FindFirstChild("HumanoidRootPart")
        if root and root.AssemblyLinearVelocity then
            local vel = root.AssemblyLinearVelocity
            local predictMs = getSliderValue("Predict ms") / 1000
            targetPos = targetPos + (vel * predictMs)
        end
    end

    local smooth = getSliderValue("Smooth")
    if smooth > 1 then
        local current = Camera.CFrame
        local targetCF = CFrame.new(Camera.CFrame.Position, targetPos)
        local lerpFactor = 1 / smooth
        Camera.CFrame = current:Lerp(targetCF, lerpFactor)
    else
        Camera.CFrame = CFrame.new(Camera.CFrame.Position, targetPos)
    end
end

RunService.RenderStepped:Connect(function()
    ScreenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    Circle.Position = ScreenCenter
    Circle.Radius = getSliderValue("Fov Size")

    local aimOn = getToggleState("Aimbot")
    local snowFov = getToggleState("Snow Fov")
    local target, dist = GetClosestPlayer()

    if aimOn and target then
        local keyPressed = false
        if aimKey ~= "" then
            local keyEnum = Enum.KeyCode[aimKey]
            if keyEnum then
                keyPressed = UIS:IsKeyDown(keyEnum)
            end
        end
        
        local useSnow = snowFov and dist <= getSliderValue("Fov Size")
        
        if keyPressed or not snowFov then
            if (not snowFov) or (snowFov and dist <= getSliderValue("Fov Size")) then
                AimAt(target)
                Circle.Color = Color3.fromRGB(0, 255, 0)
                currentTarget = target
            end
        else
            if snowFov and dist <= getSliderValue("Fov Size") then
                Circle.Color = Color3.fromRGB(255, 255, 0)
            else
                Circle.Color = Color3.fromRGB(255, 0, 0)
            end
        end
    else
        Circle.Color = Color3.fromRGB(160, 32, 240)
    end

    local espOn = getToggleState("Players")
    local showBox = getToggleState("Boxes")
    local showName = getToggleState("Names")
    local showHealth = getToggleState("Health")
    local showTracers = getToggleState("Tracers")

    if not espOn then
        for _, e in pairs(espData) do
            e.Box.Visible = false
            e.Name.Visible = false
            e.Health.Visible = false
            e.Dist.Visible = false
        end
        return
    end

    local rainbow = GetRainbowColor()
    for plr, e in pairs(espData) do
        local char = plr.Character
        if not char or not char.Parent then
            e.Box.Visible = false
            e.Name.Visible = false
            e.Health.Visible = false
            e.Dist.Visible = false
            continue
        end

        local root = char:FindFirstChild("HumanoidRootPart")
        local head = char:FindFirstChild("Head")
        if not root or not head then
            e.Box.Visible = false
            e.Name.Visible = false
            e.Health.Visible = false
            e.Dist.Visible = false
            continue
        end

        local topPos = head.Position + Vector3.new(0, head.Size.Y / 2, 0)
        local bottomPos = root.Position - Vector3.new(0, root.Size.Y / 2, 0)
        local rightOffset = root.CFrame:VectorToWorldSpace(Vector3.new(root.Size.X / 2, 0, 0))
        local leftOffset = root.CFrame:VectorToWorldSpace(Vector3.new(-root.Size.X / 2, 0, 0))
        local rightPos = root.Position + rightOffset
        local leftPos = root.Position + leftOffset

        local topV, topOn = Camera:WorldToViewportPoint(topPos)
        local bottomV, bottomOn = Camera:WorldToViewportPoint(bottomPos)
        local rightV, rightOn = Camera:WorldToViewportPoint(rightPos)
        local leftV, leftOn = Camera:WorldToViewportPoint(leftPos)

        if not (topOn or bottomOn or rightOn or leftOn) then
            e.Box.Visible = false
            e.Name.Visible = false
            e.Health.Visible = false
            e.Dist.Visible = false
            continue
        end

        local height = math.abs(topV.Y - bottomV.Y)
        local width = math.abs(rightV.X - leftV.X)
        if height < 5 then height = 5 end
        if width < 5 then width = 5 end

        local centerX = (leftV.X + rightV.X) / 2
        local centerY = (topV.Y + bottomV.Y) / 2
        local dist = (Camera.CFrame.Position - root.Position).Magnitude

        if showBox then
            e.Box.Visible = true
            e.Box.Size = Vector2.new(width, height)
            e.Box.Position = Vector2.new(centerX - width / 2, centerY - height / 2)
            e.Box.Color = rainbow
        else
            e.Box.Visible = false
        end

        if showName then
            e.Name.Visible = true
            e.Name.Text = plr.Name
            e.Name.Position = Vector2.new(centerX, topV.Y - 15)
            e.Name.Color = rainbow
        else
            e.Name.Visible = false
        end

        if showHealth then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum and hum.MaxHealth > 0 then
                local hp = math.clamp(hum.Health / hum.MaxHealth, 0, 1)
                e.Health.Visible = true
                e.Health.Size = Vector2.new(2, height * hp)
                e.Health.Position = Vector2.new(centerX - width / 2 - 4, centerY + height / 2 - height * hp)
                e.Health.Color = Color3.fromRGB(255 * (1 - hp), 255 * hp, 0)
            else
                e.Health.Visible = false
            end
        else
            e.Health.Visible = false
        end

        if showTracers then
            e.Dist.Visible = true
            e.Dist.Text = math.floor(dist) .. "m"
            e.Dist.Position = Vector2.new(centerX, bottomV.Y + 15)
            e.Dist.Color = rainbow
        else
            e.Dist.Visible = false
        end
    end
end)

local function isMobile()
    return UIS.TouchEnabled and not UIS.MouseEnabled
end

if isMobile() then
    local btnGui = Instance.new("ScreenGui")
    btnGui.Parent = CG
    btnGui.Name = "SinyxWareButton"
    btnGui.ResetOnSpawn = false

    local mobileButton = Instance.new("ImageButton")
    mobileButton.Size = UDim2.new(0, 50, 0, 50)
    mobileButton.Position = UDim2.new(1, -60, 0.5, -25)
    mobileButton.Image = "rbxassetid://73073995075962"
    mobileButton.BackgroundTransparency = 1
    mobileButton.Parent = btnGui

    local btnDrag = {active = false, start = nil, offset = nil}

    mobileButton.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            btnDrag.active = true
            btnDrag.start = input.Position
            btnDrag.offset = mobileButton.Position
        end
    end)

    mobileButton.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            btnDrag.active = false
        end
    end)

    UIS.InputChanged:Connect(function(input)
        if btnDrag.active and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - btnDrag.start
            mobileButton.Position = UDim2.new(
                btnDrag.offset.X.Scale,
                btnDrag.offset.X.Offset + delta.X,
                btnDrag.offset.Y.Scale,
                btnDrag.offset.Y.Offset + delta.Y
            )
        end
    end)

    local function toggleGUI()
        Root.Enabled = not Root.Enabled
        Circle.Visible = Root.Enabled
    end

    mobileButton.MouseButton1Click:Connect(toggleGUI)
    mobileButton.TouchTap:Connect(toggleGUI)
end

local function toggleGUI()
    Root.Enabled = not Root.Enabled
    Circle.Visible = Root.Enabled
end

UIS.InputBegan:Connect(function(i, gpe)
    if gpe then return end
    if i.KeyCode == Enum.KeyCode.LeftControl or i.KeyCode == Enum.KeyCode.RightControl then
        toggleGUI()
    end
    if i.KeyCode == Enum.KeyCode.Insert then
        toggleGUI()
    end
end)

local function getMiscState()
    local speedOn = getToggleState("Speed Hack")
    local speedVal = getSliderValue("Speed")
    local jumpOn = getToggleState("Infinite Jump")

    if LP.Character and LP.Character:FindFirstChildOfClass("Humanoid") then
        local hum = LP.Character:FindFirstChildOfClass("Humanoid")
        if speedOn then
            hum.WalkSpeed = speedVal
        else
            hum.WalkSpeed = 16
        end

        if jumpOn then
            hum.JumpPower = 50
            hum:SetStateEnabled(Enum.HumanoidStateType.Jumping, true)
        else
            hum.JumpPower = 50
        end
    end
end

RunService.Heartbeat:Connect(function()
    getMiscState()
end)

UIS.InputBegan:Connect(function(i, gpe)
    if gpe then return end
    if i.KeyCode == Enum.KeyCode.Space then
        local jumpOn = getToggleState("Infinite Jump")
        if jumpOn and LP.Character and LP.Character:FindFirstChildOfClass("Humanoid") then
            local hum = LP.Character:FindFirstChildOfClass("Humanoid")
            hum:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end
end)

local searchQuery = ""
local searchResults = {}

SearchBox:GetPropertyChangedSignal("Text"):Connect(function()
    searchQuery = SearchBox.Text:lower()
    
    for _, tab in pairs(TabContents) do
        for _, child in ipairs(tab:GetDescendants()) do
            if child:IsA("TextLabel") and child.Text ~= "" and child.Text ~= "Search" and child.Text ~= "🔍" then
                local parent = child.Parent
                while parent and parent ~= tab do
                    if parent:IsA("Frame") and parent ~= tab then
                        if searchQuery == "" then
                            parent.Visible = true
                        else
                            local found = child.Text:lower():find(searchQuery) ~= nil
                            parent.Visible = found
                        end
                    end
                    parent = parent.Parent
                end
            end
        end
    end
end)

Root.Enabled = true

-- ================================================================
--  КОНЕЦ СКРИПТА | SINYXWARE v3.0
--  Все функции работают: Аимбот, ESP, MISC, Mobile, Search
-- ================================================================
